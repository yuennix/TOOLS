{
  "name": "weyn-tools-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @workspace/api-server run dev & pnpm --filter @workspace/weyn-tools run dev",
    "build": "pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/weyn-tools run build && mkdir -p artifacts/api-server/dist/public && cp -r artifacts/weyn-tools/dist/. artifacts/api-server/dist/public/",
    "start": "NODE_ENV=production node artifacts/api-server/dist/index.js"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
